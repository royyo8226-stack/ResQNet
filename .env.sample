MONGODB_URI=
MONGODB_DB=resqnet

# Required for OTP emails (minimal setup)
SMTP_USER=
SMTP_PASS=
SMTP_PORT=587

# Optional SMTP overrides (leave empty for auto-detect)
SMTP_HOST=
SMTP_SECURE=
SMTP_FROM=